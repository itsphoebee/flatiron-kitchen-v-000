<%= form_for recipe do |f| %>
  <%=f.label :recipe_name %>:
  <%= f.text_field :recipe_name, placeholder: 'recipe_name'%> <br>

  <%= f.collection_select(:ingredient_id, Ingredient.all, :id, :name)%>
  <%= f.submit %>
<% end %>
